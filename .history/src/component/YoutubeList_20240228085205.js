import { YoutubeData } from "../ultils/YoutubeData";

const YoutubeList = () => {
  return (
    <div>
    {
        YoutubeData.map(item, index => (
            return You
        ))
    }
    </div>
  )
};
